# dotfiles

## 1. CommandLineToolsをインストールする

```bash
xcode-select --install
```

# 2. 以下のコマンドでchezmoiを実行し、アプリのインストールとdotfileの展開をする

```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/s-ikezawa/dotfiles/master/install.sh)"
```
