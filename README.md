# dotfiles

## 適応コマンド

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/s-ikezawa/dotfiles/refs/heads/main/install.sh)"
```

## Chezmoiのメモ

`.chezmoi.toml.tmpl` は `chezmoi init` の時だけ `~/.config/chezmoi/chezmoi.toml` に反映される。
