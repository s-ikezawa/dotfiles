install.sh
README.md

# vim.pack(Neovim 標準のプラグインマネージャ)が書くロックファイル。
# 置き場は $XDG_CONFIG_HOME/nvim 配下なので chezmoi の管理範囲に入ってしまうが、
# 中身は各プラグインの revision なので管理しない(`chezmoi add -r ~/.config/nvim` でも拾わない)。
.config/nvim/nvim-pack-lock.json

