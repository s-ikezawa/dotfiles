-- =============================================================================
-- Neovim Providers
-- =============================================================================
-- :help provider で各プロバイダの詳細を確認可能
-- プロバイダは外部言語ランタイムを使うプラグイン (例: deoplete, neovim-ruby) の
-- ために存在する。これらを使わないなら無効化することで以下のメリットがある:
--   - 起動時のランタイム探索コストを削減
--   - :checkhealth の警告を抑制
-- 必要になった時点で個別に有効化すればよい
-- =============================================================================

vim.g.loaded_python3_provider = 0
vim.g.loaded_ruby_provider = 0
vim.g.loaded_node_provider = 0
vim.g.loaded_perl_provider = 0
