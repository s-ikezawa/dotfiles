-- ============================================================================
-- autocmd ベースの自作機能モジュールをまとめて読み込む
-- ============================================================================
-- 単独で完結する autocmd 機能を lua/autocmds/ 配下に 1 ファイルずつ置き、
-- ここで集約して読み込む（lua/plugins/ と同じパターン）。
-- なお options.lua の UIEnter や plugins/init.lua の PackChanged のように、
-- 各モジュールの動作に付随する autocmd はそのまま各ファイル側に残す。

require("autocmds.autoreload") -- 外部変更の自動取り込み（:checktime を発火）
require("autocmds.quit-all")   -- 最後のメインエディターで :q したら補助ウィンドウごと終了
