{{ if eq .chezmoi.os "darwin" -}}
if [[ -d "$(brew --prefix eza 2>/dev/null)" ]]; then
  alias ls="eza -a"
  alias ll="eza -al --icons"
fi

if [[ -d "$(brew --prefix zoxide 2>/dev/null)" ]]; then
  eval "$(zoxide init zsh --cmd cd)"
fi
{{- end }}
