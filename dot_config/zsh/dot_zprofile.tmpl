{{- if (eq .chezmoi.os "darwin") -}}
eval "$(/opt/homebrew/bin/brew shellenv)"
# mise shims は zshenv で先頭に追加済みだが、brew shellenv が PATH を上書きで先頭追加するためここで再度先頭へ
export PATH="${XDG_DATA_HOME:-$HOME/.local/share}/mise/shims:$PATH"
{{- end }}
