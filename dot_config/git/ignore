# =============================================================
#  グローバル除外設定(~/.config/git/ignore)
# =============================================================
#  core.excludesFile を設定しなくても、git は XDG 既定でこのパスを読む。
#  「どのリポジトリでも常に無視したいもの」= 自分の環境が勝手に作るファイル
#  だけをここに書く。プロジェクト固有の除外は各リポジトリの .gitignore へ。

# macOS Finder がディレクトリを開くと自動生成する
.DS_Store

# vim/neovim のスワップファイル(異常終了時に残る)
*.swp
*.swo
