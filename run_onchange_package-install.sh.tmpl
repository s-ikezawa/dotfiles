#!/usr/bin/env bash

{{ if eq .chezmoi.os "darwin" -}}
eval "$(/opt/homebrew/bin/brew shellenv)"

brew bundle --file=/dev/stdin <<EOF
brew "chezmoi"

cask "1password"
cask "1password-cli"
EOF
{{- end }}
